prim算法是利用贪婪法进行设计的一种算法，在这里，我们假设图G是连通的。
令G=(V,E,W),其含义分别是定点，邻接矩阵的边以及相应的权值，令顶点集为
V={１,2,3,4,..n},假设与顶点i,j相关联的边为e(i,j),且e(i,j)的权重用c[i][j]
表示，Ｔ是最小生成树的边集，这个算法维护两个顶点集合S和Ｎ，开始时，令T=NULL,
S={1}, N = V - S,然后，进行贪婪算法，选取i属于Ｓ，ｊ属于Ｎ，并且c[i][j]最小
的ｉ和ｊ，并使S=S并上｛ｊ｝，N = N-{j},T = T并上{ei,j},重复上面的过程，直到
Ｎ为空，或者找到n-1条边为止，为此，可简化prim算法如下:

(1) T ＝NUll, S={0}, N = V-S

(2) 如果N为空，则算法结束；否则，转步骤(3)
(3) 寻找i属于S,j属于N,并且c[i][j]的权重最小
(4) S = S并上{j}, N = N-{j}, T=T并上{ei,j}; 转步骤(2)


算法的演示效果图为qut_prim_11和qut_prim_12
